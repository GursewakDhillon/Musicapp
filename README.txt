Install the sqlite database by npm install --save sequelize.
Run the following command:npm install && node populateDb.js && npm start
Run db.js to populate the database music.js
Open the browser on http://localhost:3000
